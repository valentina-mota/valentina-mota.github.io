const gridItem = document.querySelectorAll(".grid__item");
const gridItemTitle = document.getElementsByClassName("grid__item-title");



gridItem.forEach(function (element) {
    element.addEventListener("mouseover", function () {
        gridItemTitle.class;
    });
});